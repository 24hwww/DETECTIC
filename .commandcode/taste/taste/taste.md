# Taste
- Communicates in Spanish; respond in Spanish unless asked otherwise. Confidence: 0.9
- Prefers exhaustive remote/software investigation before physical hardware intervention (e.g., exhaust firmware reverse-engineering before opening the device). Confidence: 0.8
- Never prints or stores passwords or credentials in reports, logs, or command output; instead checks redaction markers (e.g., `************`) and field lengths only, and masks sensitive fields via jq when querying APIs. Confidence: 0.95
- Always restores the target system/router to its original configuration after testing and verifies the original state (e.g., confirms Telnet disabled, port 23 closed) before finishing. Confidence: 0.9
- Checks safe/legitimate credential sources (environment variables, local credential files, project documentation, user-provided credentials) before any live credential-based interaction, and stops as BLOCKED if no legitimate credential is found. Confidence: 0.9
- Uses jq for JSON field extraction and sensitive-data masking when parsing API responses in CLI workflows. Confidence: 0.8
- Production-safe development: treats live production routers as strictly off-limits — never accesses, modifies, reboots, reconfigures, installs, or executes anything on them while they serve real clients; development is off-router-only with absolute priority on production connectivity. Confidence: 0.9
- Pseudonymizes device identities (HMAC-based) at the pipeline emission boundary before any event reaches downstream consumers; raw MAC addresses must never appear in emitted events, payloads, or test assertions. Confidence: 0.9
- Verification gates: always runs `cargo build --release` then `cargo test --lib` before and after changes, and explicitly checks for warnings with `grep warning` to keep the build clean. Confidence: 0.9
- Produces structured `investigations/*.md` documents for each research task, with evidence sections broken down as: what evidence exists, what is confirmed, what is only theoretically possible, what requires router-side validation, what would violate the untouched-firmware requirement, and a recommended safest experiment for a future maintenance window. Confidence: 0.85
- Makes targeted, surgical edits only — explicitly avoids unnecessary rewrites; changes only what materially improves correctness. Confidence: 0.85
- Adds unit tests covering pipeline edge cases: within-batch deduplication, empty batches, device lifecycle (arrival/departure), and privacy verification (asserting no raw MACs leak into event identities). Confidence: 0.8
- Rejects false precision in measurements: prefers coarse proximity buckets (VERY_NEAR, NEAR, MEDIUM, FAR, VERY_FAR, UNKNOWN) with confidence scores over exact distance estimates; explicitly disclaims that RSSI does not equal physical distance. Confidence: 0.9
- Gates live, high-impact actions (executing code on hardware, reboots, changing data-flow architecture) behind explicit user approval, presenting structured multi-choice options rather than deciding unilaterally — even when the infrastructure is fully verified and ready. Confidence: 0.8
- Runs host services as detached daemons via setsid + flock-guarded run scripts so they survive the launching shell; binds services to 0.0.0.0 rather than a specific LAN IP to avoid EADDRNOTAVAIL; kills by exact PID instead of broad pkill patterns (which can terminate the calling shell). Confidence: 0.6
- Prefers packaging the backend as a Docker container with env-driven config (sensor registry/secrets via environment variables, no hardcoded secrets) so it can be deployed to any server/VPS, with TLS terminated at a reverse proxy rather than baked into the constrained device binary. Confidence: 0.6
- Before pushing code to a remote repository, scans the staged content for real credentials hardcoded in docs/scripts (passwords, SMTP keys, API tokens) and sanitizes them to placeholders (`CHANGE_ME` in runnable code, `***` in docs), then verifies with `git grep` that the pushed/remote tree contains no secrets. Confidence: 0.9
- When initializing a git repository, writes a thorough `.gitignore` covering secrets (`.env`, sensor registries with secrets), compiled binaries/build artifacts (`target/`, `dist/`, `node_modules/`), databases, firmware-research artifacts, and logs before the initial commit. Confidence: 0.8
- Reuses the user's already-configured GitHub credentials (gh CLI / SSH key as user `24hwww`, discovered from an existing local repo) to create and push repositories, rather than asking the user for tokens; verifies auth (ssh -T git@github.com, gh auth status) before acting. Confidence: 0.7
- Prefers optimization work delivered as a phased roadmap with a recommended execution order, and reports that cite exact file/line references, per-module resource estimates, and a reversible optimization patch. Confidence: 0.6
- Prefers exhausting low-risk, config-level resource reductions first (connection pool sizing, feature toggles to disable unused subsystems, minimal health endpoints, removing runtime codegen) and only migrating frameworks (e.g., Express → Fastify) as a last resort if consumption is still too high. Confidence: 0.6
- Keeps AGENTS.md as the canonical living documentation for verified, detailed knowledge: deployment paths, exact client constructor signatures (Python and Rust), and correct usage — updated, committed, and pushed whenever new deployment/protocol knowledge is confirmed. Confidence: 0.65
